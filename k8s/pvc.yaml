apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lancedb-storage
  namespace: rag-chatbot
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: gp2